name: Java CI with Gradle

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Start SUT
<<<<<<< HEAD
      - name: Start SUT
        run: java -jar ./artifacts/app-ibank.jar -P:profile=test &
          sleep 2

      - name: Run API tests only
        run: ./gradlew test --tests "*AuthTest" -Dselenide.headless=true --info
=======
        run: |
          java -jar ./artifacts/app-ibank.jar -P:profile=test &
          sleep 2

      - name: Run API tests only
        run: ./gradlew test --tests "*AuthTest" -Dselenide.headless=true
>>>>>>> 06931532410694b4fb6847e8f48760085b1b73bb
